are.na module :: wip
====================

currently the structs returned are plain representations of the json that the
api returns.
but i want to make custom structs that are more useful for creating software
with. for example have a User.GetChannels() function that returns all user's
channels, or Channel.GetBlocks(page, per).

done
----
functions:
GetChannel()
GetChannelContents()	// (paginated channel blocks)
GetUser()

todo
----
everything else
